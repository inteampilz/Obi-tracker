services:
  obi-tracker:
    build: .
    container_name: obi_tracker
    ports:
      - "5000:5000"
    volumes:
      # Hier wird ein dauerhafter Speicher für Einstellungen und Bilder angelegt
      - tracker_data:/app/data
    restart: unless-stopped

volumes:
  tracker_data:
