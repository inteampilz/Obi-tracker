services:
  obi-tracker:
    build: .
    container_name: obi_tracker
    ports:
      - "5000:5000"
    volumes:
      # Wir behalten nur das Volume für die Speicherdaten!
      - tracker_data:/app/data
    restart: unless-stopped

volumes:
  tracker_data:
