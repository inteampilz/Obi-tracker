version: '3.8'

services:
  multitracker:
    build: .
    container_name: multitracker_app
    ports:
      - "5005:5000"  # NEU: Wir weichen auf Port 5005 aus
    volumes:
      - multitracker_data:/app/data
    restart: unless-stopped

volumes:
  multitracker_data:
